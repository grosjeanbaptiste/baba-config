edition              = "2024"
max_width            = 100
newline_style        = "Unix"
use_field_init_shorthand = true
use_try_shorthand    = true

# `imports_granularity = "Crate"` and `group_imports = "StdExternalCrate"`
# are nightly-only rustfmt features. Re-enable when the project pins
# nightly via rust-toolchain.toml, or wait for stabilisation.
